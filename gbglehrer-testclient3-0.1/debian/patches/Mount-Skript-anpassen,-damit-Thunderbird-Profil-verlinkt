Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 gbglehrer-testclient3 (0.1-3) focal; urgency=medium
 .
   * Initial release (Closes: #nnnn)  <nnnn is the bug number of your ITP>
Author: Felix Schoppe <felix.schoppe@gbg-seelze.eu>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2020-09-16

--- gbglehrer-testclient3-0.1.orig/scripts/MountPrivateContainer.sh
+++ gbglehrer-testclient3-0.1/scripts/MountPrivateContainer.sh
@@ -18,6 +18,14 @@ else
 	mkdir $MNT
 fi
 
+#Verschlüsselten Container entschlüsseln und einhängen
+/sbin/losetup $LOOPDEV $SAFE
+/sbin/cryptsetup luksOpen $LOOPDEV $CRYPTNAME
+/bin/mount -t $FS /dev/mapper/$CRYPTNAME $MNT
+chown -R gbg:gbg $MNT
+
+#Thunderbird-Profil verlinken
+
 MAIL=/home/gbg/PrivateContainer/.thunderbird
 
 if [ -d $MAIL ]; then
@@ -26,8 +34,3 @@ if [ -d $MAIL ]; then
 else
 	echo "Thunderbird-Profil existiert nicht!"
 fi
-
-/sbin/losetup $LOOPDEV $SAFE
-/sbin/cryptsetup luksOpen $LOOPDEV $CRYPTNAME
-/bin/mount -t $FS /dev/mapper/$CRYPTNAME $MNT
-chown -R gbg:gbg $MNT
--- gbglehrer-testclient3-0.1.orig/scripts/UMountPrivateContainer.sh
+++ gbglehrer-testclient3-0.1/scripts/UMountPrivateContainer.sh
@@ -21,7 +21,7 @@ if [ -e "$MAIL" ]; then
         echo "Das Thunderbird-Profil muss nicht gesichert werden."
     fi
 else
-  echo "=> Es existiert kein Thunderbird-Profil."
+  echo "=> Es existiert kein Thunderbird-Profil!"
 fi
 
 /bin/umount $MNT
